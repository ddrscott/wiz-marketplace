[project]
name = "browser-agent-scripts"
version = "1.0.0"
description = "Browser automation scripts for Claude Code"
requires-python = ">=3.11"
dependencies = ["playwright"]

[project.scripts]
browser = "browser:main"
